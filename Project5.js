// Declare Time

// Print the Date

// Declare Current Day Element- Class = Container

// Declare Time Entries Element

// Render a block for each hour of the day 9-5
    for(var i=9; i<= 17; i++) {}





var myArray = ["alpha", "bravo"]
for(var i=0; i <= 17; i++);
// Pull the data for the hour of the loop


// Save an hour to local storage

// attempt to get the saved data for the hour of the loop
var data ="what what";

var template = `
    <div class = row?
        <div>   
            s{i}AM
        </div>
        <div>
            <textarea>${data}</textarea>
        </div>
        <div>
            <button data-hour="${i}">save</button>        
        </div>
    </div>

`

// append the html to the container element

    // st up a "click" event listener on the contaier
    containerEl.on("click", "[data-hour]", function(event) {


        // fetch the hour from the clicked buttons {}
        // event.target.matches("button");
        // fetch the hour from the clicked button's "data-hour" attribute.

        // use the hour to create the key for local storage
        // from the clicked button, traverse the DOM to the nearby <textarea> to fetch its value

    // use the key and the value to save into localStorage


    });

//   Review dom traverse.
//  Also closest button. 